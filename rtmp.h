#ifndef RTMP_HPP
#define RTMP_HPP

struct BasicHeader{};

struct MessageHeader{};

struct ExtenedTimestamp{};

struct ChunkData{};

struct ChunkHeader{};

struct __attribute__((__packed__))  RTMP_pack{
};

#endif